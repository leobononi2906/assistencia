# Supabase — configuração de projeto (usada pela CLI)
project_id = "vishxwdxqiygbxmtpfoy"

# Edge Function de credenciamento de parceiros.
# Cria o usuário no Auth já confirmado e SEM enviar e-mail (admin.createUser),
# contornando o "email rate limit exceeded" do provedor de e-mail embutido.
[functions.credenciar-parceiro]
verify_jwt = true
